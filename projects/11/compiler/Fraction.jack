class Fraction {
   field int numerator, denominator;
   field String foo, bar;
   static int count, test;

   method void gcd(int x, int y, int z) {
      var Point po;
      var int a,b;
      var String tee;

      if (~(b > 16)) {

        let a = 1;
      }
      else {
        let tee = false;
      }

      return ;
   }

}
